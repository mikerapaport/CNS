.container
  .row
    %h3 Users
    .column
      %table.table
        %tbody
          - @users.each do |user|
            %tr
              = render user
=link_to "Export users as spreadsheet", users_path(format: "xlsx")
