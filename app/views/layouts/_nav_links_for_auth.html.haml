- if user_signed_in?

  -# How do we pass the parent id to this so we go to the parent show page
  -# Need to access the parent_id from the user, what controller page is used for this page?
  -# %li= link_to 'My Parent Page', parents_path

  %li= link_to 'My Parent Page', user_path(current_user)
  %li= link_to 'Edit Account', edit_user_registration_path
  %li= link_to 'Sign Out', destroy_user_session_path, :method=>'delete'
- else
  %li= link_to 'Sign In', new_user_session_path
  %li= link_to 'Sign Up', new_user_registration_path
- if user_signed_in?
  - if current_user.try(:admin?)
    %li= link_to 'Users', users_path
